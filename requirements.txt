# Forex1998 V3 Audit Scripts — Environment
# All scripts use Python stdlib only (no external packages).
#
# Environment used for audit:
#   Python 3.11.15
#   Random seed: 42
#
# Recording the Python version preserves the full computational
# environment (stdlib-only means algorithm, seed, and code are
# sufficient for reproducibility).
